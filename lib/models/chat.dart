class Chat {
  final String image;
  final String title;
  final String lastMessage;
  final DateTime dateMessage;
  final int badge;

  Chat(
      {this.image, this.title, this.lastMessage, this.dateMessage, this.badge});
}
